# Vercel AI Gateway API key. Required for every Jev call.
# Get one at https://vercel.com/dashboard -> AI Gateway -> API Keys
AI_GATEWAY_API_KEY=

# Optional: port for the local UI server (default 3000)
PORT=3000
